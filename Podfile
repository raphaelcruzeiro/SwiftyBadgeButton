# Uncomment this line to define a global platform for your project
platform :ios, '8.0'

target 'SwiftyBadgeButton' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftyBadgeButton

  target 'SwiftyBadgeButtonTests' do
    inherit! :search_paths
    pod 'Quick', :git => 'git@github.com:sharplet/Quick.git', :branch => 'xcode-8'
    pod 'Nimble', :git => 'git@github.com:Quick/Nimble.git', :branch => 'xcode-8'
  end

end
